class course:
      def __init__(self, courseName, courseOwner, courseCode, participantList):
            self.name = courseName
            self.courseOwner = courseOwner
            self.courseCode = courseCode
            
            self.participantList = participantList
            

      def setQuizCode(self, quizCode):
            self.quizCode = quizCode

      